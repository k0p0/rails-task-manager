<div class="container">
  <div class="row">
    <div class="col-md-6 col-md-offset-3">
      <h3>Tasks detail</h3>
      <div id="task">
        <%= @task.name %>
        <!--   <% @task.status == false ? icon = "fa fa-times fa-lg" : icon = "fa fa-check fa-lg" %>
        <i class="<%= icon %>" aria-hidden="true"></i>
        -->
      </div>
      <%= link_to "Edit task", edit_task_path, class: "btn btn-success" %>
      <%= link_to "Delete task", task_path(@task), method: :delete, class: "btn btn-warning" %>
    </div>
  </div>
</div>
